// Entry module for the site. The page is fully static — there is no runtime
// data and, by design, no animation beyond the CSS `scroll-behavior: smooth`.
// This module's job is to be the bundled entry point that pulls in the
// stylesheet so Vite hashes and optimizes it for production.
import "./style.css";
